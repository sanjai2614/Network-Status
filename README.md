# 🌐 Network Status Detector

A simple web application that detects the user's internet connection status in real time using **JavaScript browser APIs**.

It shows whether the user is **Online** or **Offline** instantly when the network changes.

Live Link: https://network-status-check.vercel.app/

---

## 🚀 Features

- Real-time network status detection  
- Uses browser `online` and `offline` events  
- Instant UI update  
- Simple and clean design  
- Beginner-friendly project

---

## 🛠️ Tech Stack

- HTML  
- CSS  
- JavaScript (Vanilla JS)

---

## 🧠 How It Works

This project uses:

- `navigator.onLine` → checks current network status  
- `window.addEventListener("online")` → triggers when internet connects  
- `window.addEventListener("offline")` → triggers when internet disconnects  
